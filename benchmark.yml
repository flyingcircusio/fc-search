concurrency: 4
base: 'http://localhost:8000'
iterations: 5000
rampup: 2

plan:
  - name: Fetch overview
    request:
      url: /

  - name: Search options
    request:
      url: /search/options?q=nginx

  - name: Search packages
    request:
      url: /search/packages?q=nginx
