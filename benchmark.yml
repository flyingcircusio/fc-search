concurrency: 4
base: 'https://search.test.fcio.net'
iterations: 5000
rampup: 2

plan:
  - name: Fetch overview
    request:
      url: /

  - name: Search something
    request:
      url: /search?q=nginx
